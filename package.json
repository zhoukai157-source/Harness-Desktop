{
  "name": "dsh-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:app": "tauri build --bundles app"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
